{
  "name": "mingle",
  "version": "1.0.0",
  "main": "server/src/index.js",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "dev" : "npm run server --prefix server"
  },
  "keywords": [],
  "author": "mugunth140",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
