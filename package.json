{
  "name": "mingle",
  "version": "1.0.0",
  "main": "server/src/index.js",
  "scripts": {
    "build": "npm install --prefix server --force && npm install --prefix client --force",
    "start": "npm run start --prefix server",
    "dev" : "npm run server --prefix server"
  },
  "keywords": [],
  "author": "mugunth140",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
